// eslint-disable-next-line no-unused-vars
import React from 'react';
import useTitle from '../../hooks/useTitle';

const AddAToy = () => {
    useTitle("Add A Toy")
    return (
        <main role='main'>
            <h2>Add A Toy Section</h2>
            <section>
                
            </section>
        </main>
    );
};

export default AddAToy;